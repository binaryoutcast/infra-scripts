Usage: genhashes <version>

Prerequisites:
The binaries to hash must be located in a sibling directory to where this script is located.
That sibling directory should have the version as directory name, e.g. 28.15.0
\
+-\genhash
|
+-\28.15.0
  +-binary.exe
    binary.7z

Output: hashes.txt